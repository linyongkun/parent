eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
server:
  port: 9001
spring:
  application:
    name: core-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/share/

